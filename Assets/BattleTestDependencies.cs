using UnityEngine;

/// <summary>
/// Simple script that just disables the dependencies object for the battle
/// scene if the GameManager exists (if we're playing the game, not testing
/// </summary>
public class BattleTestDependencies : MonoBehaviour
{
//    public static BattleTestDependencies Instance;

//    private void Awake()
//    {
//#if !UNITY_EDITOR
//        // never have this enabled for builds
//        gameObject.SetActive(false);
//#else
//        // if not in a build, may be testing.
//        if (GameObject.Find("GameManager") != null)
//        {
//            gameObject.SetActive(false);
//        }
//    }
//#endif

}
